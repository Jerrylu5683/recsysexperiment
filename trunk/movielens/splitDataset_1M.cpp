#include <iostream>
#include <fstream>
#define  SOURCE "../ml10M_data/ratings.dat"
using namespace std;

int main()
{
    ifstream in(SOURCE);
    char str[200];
    while(in.getline(str) {
    
          }
}
